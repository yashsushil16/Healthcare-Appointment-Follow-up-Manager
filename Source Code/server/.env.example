PORT=5000
DATABASE_URL="file:./dev.db"
JWT_SECRET="drpatho_super_secret_jwt_key_2026_healthcare"

# Optional LLM API Key (OpenAI or Gemini). System gracefully uses built-in smart fallback engine if empty!
OPENAI_API_KEY=""
GEMINI_API_KEY=""

# Optional Email SMTP Settings (Nodemailer uses Ethereal test account automatically if blank)
SMTP_HOST="smtp.ethereal.email"
SMTP_PORT=587
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="DrPatho Appointments <no-reply@drpatho.com>"

# Optional Google Calendar OAuth 2.0 Credentials
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_REDIRECT_URI="http://localhost:5000/api/auth/google/callback"
